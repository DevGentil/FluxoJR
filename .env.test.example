# Copie para .env.test e ajuste se necessário. Usado só pelos testes de
# integração (npm run test) — nunca aponte para o banco de dev/produção. O
# nome do banco precisa conter "test" (checado em tests/global-setup.ts).
DATABASE_URL_TEST="postgresql://postgres:postgres@localhost:5432/fluxojr_test"
